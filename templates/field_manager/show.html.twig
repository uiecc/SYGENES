{% extends 'base.html.twig' %}

{% block title %}FieldManager{% endblock %}

{% block body %}
    <h1>FieldManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ field_manager.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ field_manager.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ field_manager.password }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ field_manager.phoneNumber }}</td>
            </tr>
            <tr>
                <th>ProfilePhoto</th>
                <td>{{ field_manager.profilePhoto }}</td>
            </tr>
            <tr>
                <th>Cni</th>
                <td>{{ field_manager.cni }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ field_manager.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ field_manager.lastName }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ field_manager.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ field_manager.createdAt ? field_manager.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CodeResp</th>
                <td>{{ field_manager.codeResp }}</td>
            </tr>
            <tr>
                <th>Function</th>
                <td>{{ field_manager.function }}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{ field_manager.department }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_field_manager_index') }}">back to list</a>

    <a href="{{ path('app_field_manager_edit', {'id': field_manager.id}) }}">edit</a>

    {{ include('field_manager/_delete_form.html.twig') }}
{% endblock %}
