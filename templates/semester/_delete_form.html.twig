<form method="post" action="{{ path('app_semester_delete', {'id': semester.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ semester.id) }}">
    <button class="btn">Delete</button>
</form>
