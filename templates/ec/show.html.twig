{% extends 'base.html.twig' %}

{% block title %}EC{% endblock %}

{% block body %}
    <h1>EC</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ e_c.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ e_c.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ e_c.code }}</td>
            </tr>
            <tr>
                <th>Credit</th>
                <td>{{ e_c.credit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_e_c_index') }}">back to list</a>

    <a href="{{ path('app_e_c_edit', {'id': e_c.id}) }}">edit</a>

    {{ include('ec/_delete_form.html.twig') }}
{% endblock %}
